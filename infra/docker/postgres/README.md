# PostgreSQL Local Service

The MVP uses PostgreSQL built-in full text search before embeddings.

This local init directory creates a project-specific text search configuration named `public.quant_prep_english`.

Do not add `pgvector` here during the MVP. Vector search is deferred until Phase 6.

